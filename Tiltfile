# TODO(epic-2): wire LND-mock + Postgres + Symphony stub for local stack.
# blink-card's Tiltfile is a reference but rail-specific bits differ.
print("blink-ln-gateway: Tiltfile placeholder (Story 1.1)")
